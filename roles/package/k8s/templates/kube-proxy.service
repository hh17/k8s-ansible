[Unit]
Description=Kubernetes Proxy
After=network.target
 
[Service]
EnvironmentFile=-{{nodes_path}}/cfg/kube-proxy
ExecStart={{nodes_path}}/bin/kube-proxy $KUBE_PROXY_OPTS
Restart=on-failure
 
[Install]
WantedBy=multi-user.target
